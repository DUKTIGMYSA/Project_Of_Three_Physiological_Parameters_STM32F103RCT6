/*********************************************************************************************************
* 模块名称：ADC.h
* 摘    要：ADC模块
* 当前版本：1.0.0
* 作    者：SZLY(COPYRIGHT 2018 - 2020 SZLY. All rights reserved.)
* 完成日期：2020年01月01日
* 内    容：
* 注    意：                                                                  
**********************************************************************************************************
* 取代版本：
* 作    者：
* 完成日期：
* 修改内容：
* 修改文件：
*********************************************************************************************************/
#ifndef _ADC_H_
#define _ADC_H_

/*********************************************************************************************************
*                                              包含头文件
*********************************************************************************************************/
#include "DataType.h"
#include "stm32f10x_conf.h"
#include "U16Queue.h"
#include <stdio.h>
/*********************************************************************************************************
*                                              宏定义
*********************************************************************************************************/
//#define ADC1_BUF_SIZE 100           //设置缓冲区的大小
#define ADC1_BUF_SIZE 1           //设置缓冲区的大小

/*********************************************************************************************************
*                                              枚举结构体定义
*********************************************************************************************************/

/*********************************************************************************************************
*                                              API函数声明
*********************************************************************************************************/
void InitADC(void);  //初始化ADC模块

u8   WriteADCBuf(u16 d); //向ADC缓冲区写入数据
void   ReadADCBuf(u16 *p); //从ADC缓冲区读取数据

void   ReadSPO2ADCBuf(u16 *p); //从ADC缓冲区读取数据（血氧模块）

u16 ReadECGADCBuf(u16* p); //从ADC缓冲区读取数据（心电模块）

void GetADC1Data(u16* p);  //获取ADC转换电压数据（体温模块）

void ConfigTimer3(u16 arr, u16 psc); //配置TIM3
//double   WriteADCBuf(double d); //向ADC缓冲区写入数据
//double   ReadADCBuf(double *p); //从ADC缓冲区读取数据

#endif
